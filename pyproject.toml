[project]
name = "pal-agent"
version = "0.1.0"
description = "Adaptive AI Senior Learning Mentor — Obsidian Vault knowledge graph (Phase 1)"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
pal-agent = "pal_agent.cli:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["pal_agent", "pal_agent.memory"]
